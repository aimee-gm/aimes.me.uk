.main-content {
  background: var(--white);

  .container {
    padding-top: 2rem;
    padding-bottom: 8rem;
  }

  a {
    color: var(--blue);
    text-decoration: underline;

    &:hover {
      color: var(--green);
      text-decoration: none;
    }
  }
}
